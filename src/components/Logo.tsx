import { FC } from "react";

const Logo: FC = () => (
  <div className="grid h-full place-items-center bg-primary">
    <p className="text-3xl font-semibold">The Bank</p>
  </div>
);

export default Logo;
